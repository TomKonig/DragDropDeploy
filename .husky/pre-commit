#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

if [ "$SKIP_HOOKS" = "1" ]; then
	echo "[husky] pre-commit skipped via SKIP_HOOKS=1" >&2
	exit 0
fi

echo "[husky] Running lint-staged..."
npx lint-staged || exit 1

echo "[husky] Type checking..."
npm run typecheck || exit 1
